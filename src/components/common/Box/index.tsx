import styled from 'styled-components/native';
import {
  typography,
  space,
  color,
  layout,
  flexbox,
  border,
  shadow,
} from 'styled-system';

const Box = styled.View`
  ${typography}
  ${space}
  ${color}
  ${layout}
  ${flexbox}
  ${border}
  ${shadow}
`;

export default Box;
